[build]
  command = "npm install && npm run build:client"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  COREPACK_ENABLE = "0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
