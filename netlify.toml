[build]
  command = "npm install && npm run build:client && ls -la dist"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  COREPACK_ENABLE = "0"
  NPM_FLAGS = "--version"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
